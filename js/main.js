/*
    Test de l'existence du DOM :
    Le "$(document).ready( ..." permet de s'assurer que tous les éléments du DOM sont présents avant l'exécution de notre script.
 */
$(document).ready(function() {

    // Modifier un élément :
    $('#titreCV').html('Spécialiste jQuery');

    // Insérer un contenu après l'élément sélectionné :


})



